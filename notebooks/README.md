# Notebooks

Notebooks are for data audit, feature exploration, model comparison, and communication. Reusable transformations and modelling logic should be moved into `src/nwc/` and covered by tests.

`legacy_feature_engineering.ipynb` is retained as source material and should not be treated as the production pipeline.

